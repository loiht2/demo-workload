# Placeholder Service (the demo container does not serve traffic).
apiVersion: v1
kind: Service
metadata:
  name: checkout-api
  namespace: checkout
  labels:
    app: checkout-api
spec:
  selector:
    app: checkout-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
